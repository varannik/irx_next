/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lh3.googleusercontent.com', 'https://unsplash.com'],
      },
      env: { API_URL: "https://www.quantical.dev"} 
};

export default nextConfig;


